package spreadsheet.formula.lexer;
public enum TokenType {
    NUMBER,
    REFERENCE,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    SEMICOLON,
    COLON,
    IDENT,
    LPAREN,
    RPAREN,
    EOF
}
