# TheSpreadsheet

A minimal spreadsheet app with basic formula support. Source lives in `src/main/java`, tests can live in `src/test/java`, and build output goes to `bin/` (or `out/` for VS Code).

Documentation, deliverables, diagrams, and images are collected under `doc/`.
